[core]
  pager = less -FRX

[diff]
  guitool = code

[difftool "code"]
  cmd = code --wait --diff $LOCAL $REMOTE

[init]
  defaultBranch = main

[merge]
  guitool = code

[mergetool "code"]
  cmd = code --wait $MERGED
